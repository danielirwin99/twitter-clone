import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const modaSlice = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = modaSlice.actions

export default modaSlice.reducer